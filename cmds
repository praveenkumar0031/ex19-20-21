
eksctl create cluster -f cluster.yaml

aws eks list-addons `
    --cluster-name devops-lab `
    --region us-east-1

eksctl utils associate-iam-oidc-provider `
    --cluster devops-lab `
    --region us-east-1 `
    --approve

kubectl apply -f https://github.com/kubernetes-sigs/metrics-server/releases/latest/download/components.yaml

kubectl get pods -n kube-system

kubectl get deployment metrics-server -n kube-system -o wide

kubectl describe deployment metrics-server -n kube-system

kubectl edit svc metrics-server -n kube-system
 then remove k8s/app in selector

kubectl get apiservice


helm template demo .  //show the rendered templates like plan

helm lint .
helm install demo .

helm upgrade demo .

kubectl get deployments
kubectl get pods

 eksctl create iamserviceaccount `
>>   --cluster devops-lab `
>>   --namespace kube-system `
>>   --name aws-load-balancer-controller `
>>   --attach-policy-arn arn:aws:iam::013958955427:policy/AWSLoadBalancerControllerIAMPolicy `
>>   --override-existing-serviceaccounts `
>>   --approve

aws iam get-role `
>>   --role-name eksctl-devops-lab-addon-iamserviceaccount-kub-Role1-qlBByLyN2dCW

aws iam list-attached-role-policies `
>>   --role-name eksctl-devops-lab-addon-iamserviceaccount-kub-Role1-qlBByLyN2dCW

vpc id:
aws eks describe-cluster `
  --name devops-lab `
  --region us-east-1 `
  --query "cluster.resourcesVpcConfig.vpcId" `
  --output text

  helm install aws-load-balancer-controller `
>> eks/aws-load-balancer-controller `
>> -n kube-system `
>> -f aws\alb-controller\aws-load-balancer-controller-values.yaml

kubectl logs -n kube-system aws-load-balancer-controller-dcf8758bc-xghfs

kubectl api-resources | findstr ingress
helm list -n kube-system

kubectl get hpa -w

hey -n 10000 -c 200 http://k8s-default-demoweba-6d951cbb6c-909443611.us-east-1.elb.amazonaws.com